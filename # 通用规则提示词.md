# 通用规则提示词

## 安全红线

* 文件操作必须先预览并确认，支持撤销。
* 终端命令执行前必须解释并等待批准。
* 不得收集隐私数据。
* API Key 和密码不得写入前端或日志。

## 确认规则

遇到以下情况必须停下来等待用户确认：

* 需求模糊或有多种方案
* 文件写操作/删除/移动
* 终端命令执行
* 安装新依赖
* 连续两次失败
* 需要切换模型或访问外部 API

停止格式：
"我遇到了 \[问题]，需要你确认 \[选项A/B/C]，在你回复前我不会继续。"

## Token 控制

* 只输出当前任务相关代码，优先 diff。
* 使用工具读取文件，不要求用户粘贴代码。
* 不重复需求，完成后更新 TODO 状态。
* 1\. 每次修改前，先分析相关代码，输出修改方案，等我确认后再改。
* 2\. 一次只改一个功能，改完立刻测试，不要碰其他功能。
* 3\. 涉及文件写操作、终端命令，必须停下来问我。
* 4\. 优先输出 diff 或修改片段，不要输出完整文件。
* 5\. 如果需求不明确，先问，不要猜。

