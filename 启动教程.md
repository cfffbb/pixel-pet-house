# 像素宠物小屋 - 启动教程

## 一、环境准备

### 1.1 必装软件

| 软件 | 版本要求 | 用途 |
|------|---------|------|
| JDK 17+ | Java 17 或以上 | 后端运行 |
| Maven 3.8+ | 3.8 或以上 | 后端依赖管理 |
| Node.js 18+ | 18.x 或以上 | 前端运行 |
| MySQL 8.0 | 8.0 或以上 | 数据库 |
| Git | 任意版本 | 版本管理（可选） |

### 1.2 验证安装

打开 PowerShell（或 CMD），逐条输入：

```powershell
java -version
mvn -version
node -v
npm -v
mysql --version
```

每条命令都有版本号输出即可。

---

## 二、数据库配置

### 2.1 登录 MySQL

```powershell
mysql -u root -p
```

输入你的 MySQL 密码。

### 2.2 创建数据库

```sql
CREATE DATABASE pet_ai DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE pet_ai;
SOURCE C:/Users/HP/Desktop/pet/pet-backend/src/main/resources/sql/schema.sql;
```

> 如果有迁移脚本（migration_m3 ~ migration_m13），依次执行：
> ```sql
> SOURCE C:/Users/HP/Desktop/pet/pet-backend/src/main/resources/sql/migration_m3.sql;
> SOURCE C:/Users/HP/Desktop/pet/pet-backend/src/main/resources/sql/migration_m5.sql;
> -- ...其他迁移脚本
> ```

### 2.3 修改后端配置

打开 `pet-backend/src/main/resources/application.yml`，确认数据库连接信息：

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pet_ai?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf-8
    username: root
    password: 你的MySQL密码
```

---

## 三、启动后端

### 3.1 安装依赖

```powershell
cd C:\Users\HP\Desktop\pet\pet-backend
.\mvnw.cmd compile
```

> 首次编译会下载依赖，约 3-5 分钟，请耐心等待。

### 3.2 启动后端

```powershell
cd C:\Users\HP\Desktop\pet\pet-backend
.\mvnw.cmd spring-boot:run
```

看到以下日志说明启动成功：

```
Started PetApplication in X.XXX seconds
Tomcat started on port 8080
```

> 后端运行在 http://localhost:8080

---

## 四、启动前端

### 4.1 安装依赖

```powershell
cd C:\Users\HP\Desktop\pet\pet-client
npm install
```

> 首次安装约 2-3 分钟。

### 4.2 启动开发服务器

```powershell
cd C:\Users\HP\Desktop\pet\pet-client
npm run dev
```

Vite 启动后会显示：

```
  VITE v6.0.3  ready in XXX ms

  ➜  Local:   http://localhost:5173/
```

Electron 窗口会自动弹出，显示登录界面。

---

## 五、使用系统

### 5.1 注册账号

1. 在登录界面点击"注册"
2. 填写用户名、邮箱、密码
3. 输入邮箱收到的验证码
4. 注册成功后自动跳转登录

> 管理员账号：在 MySQL 中手动将用户 role 改为 ADMIN
> ```sql
> UPDATE sys_user SET role = 'ADMIN' WHERE username = '你的用户名';
> ```

### 5.2 配置 AI 对话

1. 登录后点击底部导航栏的"设置"
2. 在"我的 AI Key"区域选择服务商（推荐 硅基流动，免费额度多）
3. 粘贴你的 API Key
4. 点击"添加"

> 带有 🎤 标记的服务商支持语音对话

### 5.3 快捷操作

| 快捷键 | 功能 |
|--------|------|
| Ctrl+Shift+M | 唤起/隐藏主窗口 |
| Ctrl+Shift+P | 唤起/隐藏悬浮宠物 |

### 5.4 系统托盘

右下角托盘图标右键菜单：
- 打开主界面
- 完整功能版
- 像素版
- 悬浮宠物
- 退出

---

## 六、打包发布

### 6.1 打包 Windows 安装包

```powershell
cd C:\Users\HP\Desktop\pet\pet-client
npm run build
```

生成的安装包在 `pet-client/dist_electron/` 目录下，双击 `.exe` 即可安装。

---

## 七、常见问题

### Q: 后端启动报数据库连接失败？

检查 `application.yml` 中的密码是否正确，MySQL 服务是否启动。

### Q: 前端启动后窗口白屏？

确认后端已启动（8080端口），Vite 开发服务器在 5173 端口。

### Q: AI 对话不回复？

检查设置面板中的 API Key 是否正确，网络是否能访问对应服务商。

### Q: 语音功能不工作？

语音需要讯飞 API 服务。在管理后台的 API 配置中填写讯飞 APPID 和 Secret。

### Q: 端口被占用？

```powershell
# 查看占用端口的进程
Get-NetTCPConnection -LocalPort 8080 -State Listen
# 杀掉对应进程
Stop-Process -Id 进程ID -Force
```

---

## 八、项目目录结构

```
pet/
├── pet-backend/          # 后端 Spring Boot
│   ├── src/main/java/com/pet/
│   │   ├── controller/   # 22个控制器
│   │   ├── service/       # 24个服务
│   │   ├── entity/        # 26个实体
│   │   ├── mapper/        # MyBatis映射
│   │   └── config/        # 配置类
│   └── src/main/resources/
│       ├── application.yml
│       └── sql/           # 数据库脚本
│
├── pet-client/           # 前端 Vue 3 + Electron
│   ├── src/
│   │   ├── components/    # 17个功能组件
│   │   ├── views/         # 3个视图
│   │   ├── api/http.js    # HTTP封装
│   │   └── soundStore.js  # 音效管理
│   ├── electron/
│   │   ├── main.js        # 主进程
│   │   └── preload.js     # 预加载脚本
│   └── package.json
│
└── generate_thesis.js    # 论文生成脚本
```
